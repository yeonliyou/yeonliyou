name: Metrics

on:
  schedule:
    - cron: "0 0 * * *"   # 매일 00:00 UTC (한국시간 09:00)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Generate metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          user: yeonliyou<img src="https://raw.githubusercontent.com/yeonliyou/yeonliyou/master/dist/metrics-6m.svg?v=20251219" width="1000">
          template: classic
          base: header, activity, community, repositories, metadata
          filename: dist/metrics-6m.svg

          plugin_calendar: yes
          plugin_calendar_limit: 6
          plugin_calendar_days: 180
          plugin_isocalendar: yes
          plugin_isocalendar_duration: half-year
